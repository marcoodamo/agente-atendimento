# =============================================================================
# AGENTE IA MULTICANAL - CONFIGURAÇÃO DE AMBIENTE
# =============================================================================
# Copie este arquivo para .env e preencha com suas credenciais reais
# cp .env.example .env

# =============================================================================
# BANCO DE DADOS (PostgreSQL + PGVector)
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# =============================================================================
# LLM - MODELO DE LINGUAGEM (LangChain + OpenAI)
# =============================================================================
# Provedor: openai ou anthropic
LLM_PROVIDER=openai

# Modelo OpenAI (recomendado: gpt-4o para OpenAI 4.1)
# Opções: gpt-4o, gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo
LLM_MODEL=gpt-4o

# Chave de API OpenAI (obrigatória se LLM_PROVIDER=openai)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Chave de API Anthropic (obrigatória se LLM_PROVIDER=anthropic)
# Obtenha em: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Configurações do LLM
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=5000

# =============================================================================
# EMBEDDINGS - VETORIZAÇÃO DE TEXTOS (RAG)
# =============================================================================
# Provedor: openai ou sentence-transformers
EMBEDDING_PROVIDER=openai

# Modelo de embedding
# OpenAI: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
# Sentence-transformers: paraphrase-multilingual-MiniLM-L12-v2 (local)
EMBEDDING_MODEL=text-embedding-3-small

# Dimensão do embedding (padrão: 1536 para OpenAI)
EMBEDDING_DIMENSION=1536

# Nota: Se usar OpenAI para embeddings, usa a mesma OPENAI_API_KEY acima

# =============================================================================
# WHATSAPP - EVOLUTION API
# =============================================================================
# URL da API Evolution (padrão: https://api.evolutionapi.com)
EVOLUTION_API_URL=https://api.evolutionapi.com

# Chave de API Evolution (obrigatória para WhatsApp)
# Obtenha em: https://evolution-api.com/
EVOLUTION_API_KEY=your_evolution_api_key_here

# Nome da instância WhatsApp configurada na Evolution API
EVOLUTION_INSTANCE_NAME=your_instance_name

# URL do webhook para receber mensagens (opcional)
# Exemplo: https://seu-dominio.com/webhook/whatsapp
EVOLUTION_WEBHOOK_URL=

# =============================================================================
# CALENDLY - AGENDAMENTOS
# =============================================================================
# Chave de API Calendly (obrigatória para módulo de agendamento)
# Obtenha em: https://developer.calendly.com/api-docs
CALENDLY_API_KEY=your_calendly_api_key_here

# Secret do webhook Calendly (para validação de webhooks)
CALENDLY_WEBHOOK_SECRET=your_webhook_secret_here

# URL base da API Calendly (padrão)
CALENDLY_BASE_URL=https://api.calendly.com

# =============================================================================
# VOZ - SPEECH-TO-TEXT E TEXT-TO-SPEECH
# =============================================================================
# Provedor: google, aws ou elevenlabs
VOICE_PROVIDER=google

# Idioma (padrão: pt-BR)
VOICE_LANGUAGE_CODE=pt-BR

# Google Cloud Speech-to-Text e Text-to-Speech
# Caminho para arquivo JSON de credenciais do Google Cloud
# Baixe em: https://console.cloud.google.com/apis/credentials
GOOGLE_APPLICATION_CREDENTIALS=path/to/google-credentials.json

# AWS Transcribe e Polly (alternativa ao Google)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1

# ElevenLabs (alternativa para TTS)
# Obtenha em: https://elevenlabs.io/
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# =============================================================================
# RAG - RETRIEVAL-AUGMENTED GENERATION
# =============================================================================
# Número de documentos relevantes a retornar na busca
RAG_TOP_K=5

# Limite mínimo de similaridade (0.0 a 1.0)
# Documentos com similaridade menor que este valor serão ignorados
RAG_SIMILARITY_THRESHOLD=0.7

# Tamanho dos chunks ao processar documentos
RAG_CHUNK_SIZE=1000

# Sobreposição entre chunks (para manter contexto)
RAG_CHUNK_OVERLAP=200

# =============================================================================
# CONFIGURAÇÕES GERAIS DO AGENTE
# =============================================================================
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Ambiente: development, staging, production
ENVIRONMENT=development

# Número máximo de mensagens no histórico de conversa
MAX_CONVERSATION_HISTORY=20

# =============================================================================
# FLAGS DE MÓDULOS (ativar/desativar funcionalidades)
# =============================================================================
# true ou false (minúsculas)
ENABLE_AGENDAMENTO=true
ENABLE_FOLLOWUP=true
ENABLE_VOZ=true
ENABLE_KNOWLEDGE=true
ENABLE_TRANSBORDO_HUMANO=true

# =============================================================================
# API - AUTENTICAÇÃO E CONFIGURAÇÃO
# =============================================================================
# API Key para autenticação dos endpoints da API
# Gere uma chave segura (ex: openssl rand -hex 32)
API_KEY=

# Habilitar autenticação (true/false)
# Em desenvolvimento, pode ser false para facilitar testes
ENABLE_API_AUTH=true

# Título e versão da API
API_TITLE=Agente IA Multicanal
API_VERSION=0.1.0

# =============================================================================
# CONFIGURAÇÕES OPCIONAIS AVANÇADAS
# =============================================================================
# Redis (para cache e filas - opcional)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# Celery (para tarefas assíncronas - opcional)
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# 2. Mantenha suas chaves de API seguras e não as compartilhe
# 3. Para produção, use variáveis de ambiente do sistema ou gerenciador de secrets
# 4. Algumas configurações são opcionais dependendo dos módulos que você usa
# 5. Para desenvolvimento local, você pode usar valores padrão para PostgreSQL
# 6. Certifique-se de que o PostgreSQL está rodando antes de iniciar o agente

# =============================================================================
# CONFIGURAÇÃO MÍNIMA NECESSÁRIA
# =============================================================================
# Para funcionar básico, você precisa configurar pelo menos:
# - OPENAI_API_KEY (obrigatória)
# - POSTGRES_PASSWORD (ou usar padrão)
# 
# Para WhatsApp:
# - EVOLUTION_API_KEY
# - EVOLUTION_INSTANCE_NAME
#
# Para Agendamentos:
# - CALENDLY_API_KEY
#
# Para Voz:
# - GOOGLE_APPLICATION_CREDENTIALS (Google) OU
# - AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY (AWS) OU
# - ELEVENLABS_API_KEY (ElevenLabs)

